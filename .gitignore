# Claude Code local session artifacts
.claude/

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Xcode — user-specific
xcuserdata/
*.xcuserstate
*.xcuserdatad/
**/xcshareddata/WorkspaceSettings.xcsettings

# Xcode — build products
build/
DerivedData/
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# Swift Package Manager
.build/
.swiftpm/
Package.resolved

# CocoaPods / Carthage (unused now, ignored for safety)
Pods/
Carthage/Build/

# Fastlane
fastlane/report.xml
fastlane/screenshots

# Secrets / signing (never commit)
*.mobileprovision
*.p12
*.cer

# Firebase config — contains API key; obtain from Firebase console
GoogleService-Info.plist

# Firebase CLI local deploy cache
.firebase/
