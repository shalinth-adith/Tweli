
## 2026-07-12T18:30:00Z — /plan-feature / Completion
**What was done**: Generated full technical specs for 18-firebase-migration (MVP mode, agent team: database/api/ui + index/tasks/validator).
**Decisions made**: Pair-code-as-invite (no share URL); single spaces/{id} doc + 6 item subcollections with JSON-string payloads; dual identity (Firebase UID for membership/rules, app UUIDs stay in payloads); snapshot listeners replace change tokens; leave-space is local-only; background push deferred (Cloud Functions = Blaze); cutover with no data migration.
**Blockers**: None. Validation PASS after 5 spec amendments (W1–W5 resolved, see validation-report.md Resolutions).
**Next steps**: User does Firebase console setup tasks (SETUP-*), then /implement docs/planning/features/18-firebase-migration.
**Files changed**: docs/planning/features/18-firebase-migration/{requirements,database,api-endpoints,ui-components,FEATURE_INDEX,tasks,validation-report}.md, feature-status.json

## 2026-07-14T10:30:00Z — /implement / Completion
**What was done**: Implemented 18-firebase-migration (MVP mode). CloudKit fully replaced by FirebaseService (Firestore+Auth+FCM); rules deployed to tweli-9a99e; 3/3 tests pass; verification PASSED WITH WARNINGS (3 screenshots).
**Decisions made**: tweli://join?code links (Hosting deferred); leave-space local-only; DEBUG dev bypass short-circuits Firestore; iPhone 17 sim for tests.
**Blockers**: TEST-3/TEST-4 deferred to manual (need two real Apple sign-ins). Env: repo under iCloud Desktop breaks codesign on SwiftPM bundles — use external DerivedData or move repo.
**Next steps**: User runs two-device invite + deletion-propagation manual tests on a non-debug build.
**Files changed**: Tweli/Services/FirebaseService.swift (new), AuthService, AppDelegate, TweliApp, AppViewModel, PendingInvite, 7 services, 4 views, entitlements, pbxproj (SPM), firestore.rules/firebase.json (deployed); CloudKitService + CloudSharingSheet deleted.

## 2026-07-21 — Mood interstitial + strip (designs 21a/b, 22a/b)
**What was done**: Built the "new mood" interstitial: a full-screen swipeable card over a dimmed/scaled Home shown when the partner posts a mood newer than last-seen. Right swipe → Moods tab; left swipe / × / scrim → stays Home. Collapses to a tiny strip at top of Home (tap → Moods). Silent (nothing sent). Strip REPLACES the partner side of the Home hero; hero is now just "You feel".
**Decisions made**: right=Moods, left=home (per user, replaces design's KEEP/DISMISS-to-moments). No Moments store built. Freshness via MoodService.freshPartnerMood vs persisted lastSeenPartner date; acknowledge on dismiss.
**Files changed**: MoodInterstitialView.swift (new), MoodStripView.swift (new), MoodService.swift (freshPartnerMood/acknowledge), AppViewModel.swift (moodInterstitial + maybe/dismiss), MainTabView.swift (overlay + scale + scenePhase trigger), HomeMomentView.swift (strip replaces partner hero), MockData.swift (DEBUG demo note).
**Verification**: BUILD SUCCEEDED (iPhone 17 sim, external DerivedData). Interstitial + strip render pixel-accurate to 22b/21b (clean screenshots). Could NOT exercise the swipe gesture or live "suppress-after-seen" — this env has no simulator touch injection (no idb/accessibility). Suppression is a plain timestamp comparison, correct by inspection.
**Next steps**: Manual swipe test on device. Consider: notification-permission prompt races the interstitial on first-ever launch (edge case).

## 2026-07-21 — Home redesign to designs 21a (light) / 21b (dark)
**What was done**: Rebuilt Home to match 21a/21b exactly. The partner's fresh mood is now an INLINE swipeable card on Home (gradient top bar, pulsing "Just updated", partner avatar + mood + quote, lock "Swipe away — just for you, they won't know" footer) that collapses in place to the existing MoodStripView. Removed the full-screen MoodInterstitialView overlay entirely. Home is now: greeting header → fresh-mood card/strip → "Today" reminders. Removed the "You feel" gradient hero, the "Until we meet again" countdown card, and the Send love / Plan a date action row (per user: "match 21a/21b exactly").
**Decisions made**: Renamed AppViewModel.moodInterstitial→freshMood, maybeShowMoodInterstitial→revealFreshMoodIfAny, dismissMoodInterstitial→dismissFreshMood (behaviour identical, just inline now). Swipe right→Moods tab, swipe left→collapse to strip (silent, acknowledges). Header greeting now "Weekday · good evening" (design's "Tuesday · good evening"). Neutral pronoun in footer ("they won't know") instead of the mockup's "she". Removing the countdown card stranded CountdownView (its only entry point) → preserved it by making PartnerSpaceView's countdown card a NavigationLink into CountdownView. HomeRoute.missingYou was already dead code (no nav to it) — left as-is.
**Verification**: BUILD SUCCEEDED (iPhone 17 sim, external DerivedData — iCloud codesign workaround). Captured light + dark screenshots; both pixel-match 21a/21b. Could NOT exercise the swipe (no sim touch injection). Added DEBUG-only launch hook AppEnvironment.applyLaunchOverridesIfNeeded() driven by SIMCTL_CHILD_TWELI_DEMO=1 / TWELI_FRESH_MOOD=1 so a headless build boots straight to a connected Home for screenshots (compile-excluded from Release).
**Files changed**: FreshMoodCardView.swift (new, replaces MoodInterstitialView.swift which was deleted), HomeMomentView.swift (stripped to card/strip + reminders), AppViewModel.swift (rename), MainTabView.swift (removed overlay + scale), HomeView.swift (weekday greeting + onAppear reveal), PartnerSpaceView.swift (countdown → tappable), AppEnvironment.swift + TweliApp.swift (DEBUG launch hook).
**Next steps**: Manual swipe test on device (right→Moods, left→strip). Confirm the removed countdown/action entry points are acceptable, or resurface elsewhere if missed.

## 2026-07-21 (correction) — Home card is STATIC; interstitial restored
**What was done**: User clarified the earlier step was wrong: the full-screen swipe interstitial (22a/b) must STAY, and the Home 21a/b card must NOT be swipeable — it's the calm resting state you land on AFTER swiping the interstitial away.
  - Restored MoodInterstitialView.swift (verbatim) + MainTabView's overlay/scale/scenePhase wiring (now reading app.freshMood / dismissFreshMood).
  - Rewrote FreshMoodCardView as a STATIC, tappable card (Button → Moods tab): removed the drag gesture/fling, swapped the « » swipe chevrons for a single chevron.right, and changed the footer from "Swipe away — just for you, they won't know" to "Only you — they won't know you saw this".
  - HomeMomentView now shows the static card whenever moods.partnerMood != nil (resting state). MoodStripView is no longer referenced anywhere (dead file — left in place, candidate for deletion).
  - Extended the DEBUG launch hook: without TWELI_FRESH_MOOD it now marks the mock mood seen (lastSeenPartner = now) so Home shows the calm card with no interstitial; with the flag it clears the baseline so the interstitial raises.
**Verification**: BUILD SUCCEEDED. Screenshots: (1) resting Home dark — static card, single chevron, new footer, no interstitial ✓; (2) fresh-mood dark — interstitial raises over dimmed/scaled Home (its NEW MOOD header, × button, Home/Private/Moods footer visible; center overlapped by the one-time iOS notification prompt on fresh install). Swipe still needs manual on-device test (no sim touch injection).
**Files changed**: MoodInterstitialView.swift (recreated), FreshMoodCardView.swift (de-swiped), MainTabView.swift (overlay restored), HomeMomentView.swift (static card), AppEnvironment.swift (hook tweak).

## 2026-07-21 — Reunion countdown chip + "When do you meet?" sheet (updated 21a/b)
**What was done**: The updated 21a/b design adds a ❤️ N-days countdown chip to the Home mood card; tapping it opens a "When do you meet?" half-sheet (calendar) to set the reunion date.
  - FreshMoodCardView: added the ❤️ chip to the header (replaces the tap chevron). Chip = "N days" from the pinned .meeting countdown, or "Set date" if none. Card now has two tap targets — chip (→ sheet) via Button, card body (→ Moods) via onTapGesture. Footer copy → "Thinking of you across the miles ♡" (per design).
  - MeetDateSheetView (new): custom month calendar (prev/next nav, today/past styling, pink selected day), "N days to go · date" summary bar, gradient "Set meet date" button. Presented at .height(580) half-sheet. On confirm: updates the existing .meeting countdown or creates a new pinned one (title "Until we meet again") via CountdownService — reuses widget + day-zero notification + Firestore sync.
  - HomeMomentView: re-added CountdownService, presents the sheet, passes reunionDays. DEBUG-only TWELI_MEET_SHEET=1 launch flag auto-opens the sheet for headless screenshots.
**Verification**: BUILD SUCCEEDED. Screenshots light + dark of both the chip ("21 days") and the sheet (Aug 11 preselected, "21 days to go", consistent with today Jul 21 + 21). Tests re-running. Tapping the chip / picking a date still needs manual on-device confirmation (no sim touch injection), but rendering + wiring verified.
**Files changed**: FreshMoodCardView.swift, MeetDateSheetView.swift (new), HomeMomentView.swift.
**Next steps**: Not yet committed. Manual: tap chip → pick date → confirm chip updates.

## 2026-07-21 — Auto-location + partner distance ("N km apart", designs 21a/b)
**What was done**: Built the location→distance engine and wired the "N km apart" row onto the Home mood card.
  - Engine (net-new; app had zero geo before): SharedLocation model + LocationService (mirrors MoodService — one record per user, myLocation/partnerLocation by id). Owns a CLLocationManager (coarse/kCLLocationAccuracyKilometer), opt-in requestAndCapture(), reverse-geocode via CLGeocoder for a city label, refreshIfStale() (>1h) called on app foreground + startup. Distance computed on-device via CLLocation.distance; pure static helpers distanceMeters/distanceLabel (MeasurementFormatter .naturalScale, km/mi per locale).
  - Sync: added RType.location = "locations" to FirebaseService (+saveLocation/deleteLocation); auto-wires the listener (RType.all). AppViewModel: locationService in graph, wireIdentities, onDataChanged→widget, applyRemoteChanges mergeRemote. firestore.rules: added 'locations' to isKnownItemType() allowlist (NEEDS REDEPLOY: firebase deploy --only firestore:rules).
  - Permission: NSLocationWhenInUseUsageDescription in Info.plist.
  - About-you: "Use my location" button (location.fill) in the city field → requestAndCapture; onChange(myLocation.cityLabel) auto-fills the city; manual entry stays as fallback.
  - Display (21a/b): FreshMoodCardView footer now shows a distance row (pink mappin, "N km apart" + "PartnerCity ↔ YourCity" + refresh icon + "Nh ago"). Falls back to "Thinking of you across the miles ♡" when distance is nil (before both share location). HomeMomentView computes FreshMoodDistance from LocationService.
  - Demo: MockData.locations (London ↔ Chennai) seeded in LocationService under useDemoData.
**Verification**: BUILD SUCCEEDED; 4/4 unit tests pass (added partnerDistanceMath + SharedLocation round-trip). Light + dark screenshots show "8,217 km apart · Chennai ↔ London · 1 hr ago" — a REAL computed value, not hardcoded. App launches cleanly with the new CLLocationManager. Live capture (permission prompt → GPS → geocode) not exercisable headlessly (no touch injection / sim location) — verified by inspection; demo path proves compute+display+merge.
**Files**: NEW Models/SharedLocation.swift, Services/LocationService.swift. MOD FirebaseService, AppViewModel, TweliApp, MainTabView, Info.plist, firestore.rules, MockData, Views/Auth/AboutYouView, Views/Home/FreshMoodCardView + HomeMomentView, TweliTests.
**Next steps**: Deploy firestore rules. Manual on-device: tap "Use my location" → grant → city fills + real distance to partner. Not yet committed.

## 2026-07-22 23:59 — /debug / Completion — partner cannot join space
**Symptom**: Second user (partner device) stuck on Welcome; Join always failed with the generic network error.
**Root cause**: firestore.rules only allowed `get` on spaces/{spaceId} for existing members, but joinSpace's transaction READS the space doc before its atomic update — joiner is not a member yet → PERMISSION_DENIED → join could never succeed. Rules on disk were also undeployed (drift).
**Fix applied**: rules now allow a signed-in non-member to `get` a space with an open seat (memberUids.size() < 2); joinSpace maps permissionDenied → .spaceFull (honest copy). Deployed rules to tweli-9a99e (also picked up pending 'locations' rule).
**Verified**: BUILD SUCCEEDED (iPhone 17 Pro Max sim, external DerivedData); 4/4 TweliTests pass; rules compiled + released by firebase CLI. Two-device invite E2E still needs a real-device retry by the users.
**Files changed**: firestore.rules, Tweli/Services/FirebaseService.swift, docs/planning/TROUBLESHOOTING.md (new).
**Next steps**: Partner retries the invite code (existing code still valid unless >48h old). Not yet committed.

## 2026-07-23 15:10 — /debug / Partner push notifications — Cloud Function built (not yet deployed)
**Symptom**: No notification when partner updates mood/reminders/etc. — only works while app open (snapshot listeners).
**Root cause**: Client was fully wired (APNs entitlement, FCM token stored on space doc, authorUid stamped on every item write, didReceiveRemoteNotification handler) but NO server ever sent a push. Comments said "background push deferred to Blaze". Missing piece = a Cloud Function sender.
**Work done**: Added functions/ (Node 22, firebase-functions v6, firebase-admin v12). Single onDocumentWritten trigger `spaces/{spaceId}/{type}/{itemId}` finds the non-author member, reads fcmTokens[recipient], sends FCM. Per-type copy: moods (create+update), pings, reminders (create + completion only; skips assignedTo==me), countdowns/letters/virtualDates (create), locations ignored. Dead-token pruning on send failure. Wired into firebase.json + .gitignore. npm install OK, module loads + registers, copy sanity-checked.
**BLOCKED on user (external)**: (1) Enable Blaze plan (Cloud Functions require it; ~$0 at 2-user scale). (2) Upload APNs Auth Key (.p8 + Key ID + Team ID) to Firebase Console → Cloud Messaging → Apple app config — without it FCM cannot reach iOS at all.
**Next steps**: After Blaze + APNs key, run `firebase deploy --only functions`, then two-device E2E: partner changes mood → other device (app backgrounded) gets banner. Not yet committed.
**Files changed**: functions/index.js (new), functions/package.json (new), firebase.json, .gitignore.

## 2026-07-23 15:40 — /debug / Partner push — DEPLOYED
**Done**: Blaze enabled by user; APNs auth key uploaded to both dev+prod slots. Moved function region us-central1 → asia-south1 to collocate with Firestore (Mumbai). First deploy hit the known first-time-2nd-gen Eventarc propagation error; retry succeeded. Set Artifact Registry cleanup policy (3-day) to avoid image storage creep.
**State**: `notifyPartnerOnItemWrite` live (v2, firestore.document.written, asia-south1, nodejs22). functions:list confirms.
**Next**: Two-device E2E — partner updates mood with the other device backgrounded → expect banner. Watch `firebase functions:log`. Not yet committed.

## 2026-07-24 — /debug / Timezone handling for push + reminders
**Context**: Long-distance couple in different timezones. Two fixes shipped.
**Part A — Quiet hours (silent overnight), DEPLOYED**: Client now writes `memberTimezones[uid]` to the space doc (FirebaseService.updateMyTimezone, called in AppViewModel.refresh after updateMyMemberName). No rules change needed — isMemberEdit permits the partial map write (same path as fcmTokens). Cloud Function reads recipient's IANA tz; if their local hour ∈ [22,08) → sends with aps interruption-level "passive", no sound (banner lands, no buzz). Fails open if tz missing/invalid. Redeployed to asia-south1. Verified: quiet-hour calc correct for Kolkata/NY/London/Sydney/invalid.
**Part B — Partner-assigned reminders fire at RECIPIENT's wall clock, BUILD+TESTS PASS**: Added ReminderItem.authorTimezone (stamped at build via AddReminderViewModel) + localFireDate (reinterprets the author's wall-clock components into the current device zone). isToday/isUpcoming/isMissed/timeLabel now use localFireDate. ReminderNotificationService.schedule reads components in the author's zone so the trigger fires that wall clock in the device's local zone. ReminderService gained shouldRing (assignment gate: .me→creator only, .partner→other only, .both→both) + applySchedule (idempotent ring/cancel); all schedule/reschedule/mergeRemote/toggle/snooze/reschedule routes through it. snooze/reschedule re-anchor authorTimezone to current (absolute intent). Same-timezone couples unaffected (no-op). Also fixes prior bug where BOTH devices rang for one-sided assignments.
**Verified**: BUILD SUCCEEDED; TEST SUCCEEDED (5/5 unit incl. new reminderWallClockAcrossTimezones; UI tests pass). Gate correctness relies on currentUser.id being persisted (UserDefaults tweli.currentUser) — confirmed stable.
**NOT committed yet**. Files: functions/index.js, Tweli/Services/FirebaseService.swift, Tweli/App/AppViewModel.swift, Tweli/Models/ReminderItem.swift, Tweli/ViewModels/AddReminderViewModel.swift, Tweli/Services/ReminderNotificationService.swift, Tweli/Services/ReminderService.swift, TweliTests/TweliTests.swift.
**Next**: two-device cross-TZ manual test; commit push + timezone work.

## 2026-07-24 — Design deltas: type-mood, swipe Keep/Dismiss, location diagnosis, zero-data
**Source**: user's "Twinderly iOS widget design.zip" (Tweli Final.dc.html) screens 21a/b,22a/b,24a/b. Decisions: zero-data=guarantee-no-seed (non-destructive); swipe=Right-Keep/Left-Dismiss; location=both-granted-but-nothing (real bug).
**24a/b type-your-own-mood — DONE (build+tests pass)**: MoodStatus gained customText:String? + displayLabel (customText ?? mood.label). setMyMood(_:customText:note:) normalizes empty→nil. MoodSharingView: added "Type your own mood…" pill input (pencil icon, 24-char cap), previewLabel drives the big preview, chips deselect when custom typed & tapping a chip clears custom, restore existing customText onAppear. All display sites route through displayLabel: FreshMoodCardView, MoodInterstitialView, MoodStripView, widget snapshot (AppViewModel.refreshWidget). Emoji surface keeps preset fallback.
**22a/b swipe Keep/Dismiss — DONE (build pass)**: MoodInterstitialView: onOpenMoods→onKeep; right=KEEP (green heart stamp), left=DISMISS (grey × stamp); footer buttons replaced with swipe-hint text + "Only you can see how you respond". AppViewModel.dismissFreshMood(openMoods:)→(keep:). MoodService: setPartnerMoodKept + partnerMoodCollapsed (persisted key tweli.mood.collapsedToStrip, keyed to mood.updatedAt so a newer mood resets to card). HomeMomentView: shows FreshMoodCardView when kept, MoodStripView when collapsed. ASSUMPTION: Keep=big card, Dismiss=strip (design showed both; not explicitly stated).
**Location "both granted but nothing" — DIAGNOSTIC ADDED (not root-caused)**: code path verified correct (moods sync proves the save/listen/merge channel; matching mirrors moods). Added DEBUG [Location] lifecycle logging (requestIfNeverAsked/refreshIfStale/captureNow/didUpdateLocations/didFail/authChange/setMyLocation/mergeRemote + logState snapshots) and a save ok/SKIPPED/failed log in FirebaseService.save. NEXT: user runs Debug build on both devices, greps console for [Location], shares → pinpoint break.
**Zero-data — VERIFIED, no change**: all MockData seeding is #if DEBUG && AppEnvironment.useDemoData; release is compile-time false. Already guaranteed. User's visible data is real Firestore test records.
**OPEN**: 21a/b "wire everything" — unclear what's unwired (home card + sheets + taps already built); need user specifics. NOT committed.

## 2026-07-24 — Home rebuilt to match designs 21a/b (light+dark)
**What was done**: Home dashboard now faithfully realizes 21a/b. Prior card had crammed the meet-date countdown chip + distance row into the mood card; design keeps those as separate siblings. Decomposed into: (1) slimmed FreshMoodCardView — "From <partner>" eyebrow, avatar + "<partner> feels" + big mood + hairline + italic note ONLY (removed chip, distance footer, accent bar, pulsing dot, FreshMoodDistance struct, daysRemaining/distance/onCountdownTap params); (2) NEW ClosenessStripView — blue band "N km apart" (location.distanceApartLabel) + "N days together" (couple.coupleSpace.createdAt); (3) NEW DatesCardView — "Dates"/"Open all", calendar tile + virtualDates.next title + "Sat, Jul 12 · in 2 days", taps → Dates tab (requestedTab=2); reminders eyebrow "Reminders · Today"→"Today" + added "Add a reminder" row (→Reminders tab, requestedTab=1). Added Color.twInfo (systemBlue). Header greeting "·"→"," and avatar 42→38px gradient to match.
**Decisions made**: Meet-date sheet no longer has a home entry point (not in 21a/b); still reachable via DEBUG TWELI_MEET_SHEET hook + code retained. Dates card empty state = "Plan your first date" prompt, still taps to Dates tab.
**Verification**: xcodebuild BUILD SUCCEEDED (iPhone 17 sim, external DerivedData). Booted demo (TWELI_DEMO=1 + TWELI_FRESH_MOOD=1), screenshotted BOTH dark (21b) and light (21a) — every element present, correct order, no overflow/contrast issues. Screenshots in scratchpad home-21a.png / home-21a-light.png.
**Files changed**: FreshMoodCardView.swift (rewrite), HomeMomentView.swift (rewire), ClosenessStripView.swift (new), DatesCardView.swift (new), HomeView.swift (header), DesignSystem.swift (twInfo).
**Next steps**: user review; commit when approved. Files auto-included via PBXFileSystemSynchronizedRootGroup (no pbxproj edit).

## 2026-07-24 — Remove Dates tab → Home "Dates" card opens a half-sheet
**What was done**: Dates is no longer a tab. MainTabView is now 4 tabs (Home 0 · Reminders 1 · Moods 2 · Letters 3). New DatesSheetView (Tweli/Views/Dates/) = the old planner repackaged as a .presentationDetents([.medium,.large]) sheet with a NavigationStack inside (Done + "Dates" + "+" toolbar, Next-date hero, Planned list, row→VirtualDateDetailView push, "+"→AddVirtualDateView, context-menu complete/cancel). HomeMomentView's DatesCardView onOpen now sets showDatesSheet=true (was requestedTab=2). Deleted VirtualDatePlannerView.swift (removed screen).
**Tab-index ripple (Moods 3→2)**: updated requestedTab=3→2 in AppViewModel:74 (widget sendlove/mood deep link), MoodStripView, HomeMomentView mood-card onTap. Reminders stays 1.
**Debug hook**: added TWELI_DATES_SHEET=1 (alongside TWELI_MEET_SHEET) in HomeMomentView #if DEBUG onAppear to auto-open the sheet for headless screenshotting.
**Verification**: BUILD SUCCEEDED (iPhone 17 sim, external DerivedData). Screenshotted the half-sheet (dates-sheet.png — medium detent over dimmed Home, hero + Planned rows) and clean Home (home-4tabs.png — 4-tab bar, Dates card present). Light+dark both fine.
**Files changed**: MainTabView.swift, AppViewModel.swift, MoodStripView.swift, HomeMomentView.swift, DatesCardView.swift (doc), DatesSheetView.swift (new); deleted VirtualDatePlannerView.swift.
**Next steps**: user review; commit when approved.

## 2026-07-24 — Restore reunion-date setting (lost when mood card was slimmed)
**Problem**: Slimming FreshMoodCardView to match 21a/b removed the ❤️ countdown chip, the only entry point to MeetDateSheetView — so "days remaining"/setting the meet date became unreachable (user report).
**Fix**: Moved the reunion control onto ClosenessStripView. Strip is now a Button(onSetMeet). Left = 🌐 "N km apart" + 💗 "N days together" (quiet caption). Right = reunion control in accent: ✈️ "N days to go ›" when a meet date is set, "calendar.badge.plus Set meet date ›" when not. New params daysToReunion + onSetMeet. HomeMomentView re-added reunionDays (meeting-category countdown ?? pinned) and passes onSetMeet:{ showMeetSheet=true }. MeetDateSheetView unchanged (already functional; creates/updates pinned .meeting CountdownItem).
**Verification**: BUILD SUCCEEDED. Screenshotted Home (strip shows "21 days to go") + meet sheet opening with Aug 14 pre-selected. Files: ClosenessStripView.swift, HomeMomentView.swift.

## 2026-07-24 — Closeness strip: two tap targets (animated distance + shared meet date)
**Decision (AskUserQuestion)**: right segment = reunion countdown (drops "days together"); tap → meet-date picker, syncs to both.
**Left "N km apart" → animated distance screen**: new DistanceJourneyView (fullScreenCover). Night-sky gradient + stars, glowing indigo→pink quadratic-bézier arc between the two cities, an "airplane" travelling the arc (progress 0→1, hides at ends), endpoints "YOU · <city>" / "<partner> · <city>", big gradient distance + "apart · as the crow flies" + "<me> ❤️ <partner>". Takes plain values (no env). Verified via TWELI_DISTANCE=1: London,UK ↔ Chennai,India, 8,217 km.
**City/Country**: LocationService geocode label now "City, Country" (place.locality ?? subAdmin ?? admin, + place.country) instead of city-or-state. MockData cities → "London, UK" / "Chennai, India".
**Right "N days to go" → meet date, shared**: reuses MeetDateSheetView (unchanged). Sync is FREE — CountdownService.update→cloud.saveCountdown→Firestore, syncNow() merges partner copy (same channel as all countdowns). No backend change.
**ClosenessStripView**: rebuilt as two Buttons split by a divider (matches user's screenshot). Params now distanceLabel, daysToReunion, onOpenDistance, onSetMeet (removed daysTogether). Left half hidden when distanceLabel==nil.
**HomeMomentView**: added showDistance + fullScreenCover(DistanceJourneyView); removed daysTogether/couple env; FIXED mood-card onTap requestedTab 3→2 (was opening Letters after Dates-tab removal); added TWELI_DISTANCE debug hook.
**Verification**: BUILD SUCCEEDED (iPhone 17 sim). Screenshots: strip2.png (two-segment strip), distance.png (animated journey).
**Files**: DistanceJourneyView.swift (new), ClosenessStripView.swift, HomeMomentView.swift, LocationService.swift, MockData.swift.

## 2026-07-24 — Distance screen REDESIGNED to match design (distL/distD)
**Correction**: my first distance screen was a full-screen night-sky arc — WRONG. Found the real design in the updated zip (re-extracted; zip was refreshed 14:34): a LIGHT half-sheet "The distance between us".
**DistanceJourneyView rebuilt 1:1 from the 320×190 SVG**: half-sheet (.presentationDetents([.fraction(0.62),.large])); header title + "CityA → CityB" subtitle + close X; map card (lavender→pink gradient #EAF2FF→#FDEEF3, blue border) with dashed grey underlay route + animated blue cubic route (M72 150 C140 150 158 66 250 66, trim 0→1), filled teardrop MapPinShape (blue=you bottom-left, red=partner top-right) tips anchored to route ends, pulsing destination glow, SF airplane flying the path (position=cubic(t), rotation=tangent−45°), city labels; three white stat tiles km apart / days together / days to go (#0A84FF / #FF2D55 / #E23B5A). Subtitle+pins show city only (split cityLabel on comma). Presented via .sheet now (was fullScreenCover).
**HomeMomentView**: re-added couple env + daysTogether; DistanceJourneyView(myCity,partnerCity,distanceLabel,daysTogether,daysToGo).
**Verification**: BUILD SUCCEEDED. Screenshot dist4.png matches design (London→Chennai, plane oriented along route, tiles 8,217/10/21).
**Files**: DistanceJourneyView.swift (rewrite), HomeMomentView.swift.

## 2026-07-24 — Distance sheet: flat map → 3D GLOBE (design updated again, zip 15:15)
**Design change**: distL/distD map card is now a stylized 3D globe (viewBox 300×264, globe @ (150,138) r104). Subtitle now full "City, Country → City, Country".
**New GlobeCanvas.swift**: Canvas draws the globe 1:1 — blurred atmosphere halo (#9AD1FF), radial-gradient ocean sphere (#7EC0F5→#3E8FD6→#1E63AE→#144E8C, light origin 40%/32%), green continents (#69A85A/#4E8A44) via SVG parser, blurred clouds, lat/long graticule (white .28), spherical shade overlay (→rgba(4,18,40,.6) edge), specular highlight. Clipped to sphere via ctx.drawLayer{ clip }.
**New SVGMiniPath.swift**: minimal SVG `d`→SwiftUI Path parser (M/m L/l H/h V/v C/c Q/q Z/z) so continents reproduce exactly. Reusable.
**DistanceJourneyView rebuilt**: 300×264 art ZStack (GlobeCanvas + dashed route + golden trimmed route M188,168 Q165,74 118,116 + pulsing destination glow + SF-airplane flying quad(t) rotated tangent−45°, now stays at destination + teardrop pins tip-anchored + white-halo city labels), scaled to fit card via GeometryReader. Subtitle uses full cityLabel; pins/labels use city-only (split on comma). Header + 3 tiles unchanged (km apart/days together/days to go).
**Verification**: BUILD SUCCEEDED. Screenshot globe.png — faithful: 3D globe, London(blue)→Chennai(red) route, tiles 8,217/10/21.
**Files**: GlobeCanvas.swift (new), SVGMiniPath.swift (new), DistanceJourneyView.swift (rewrite).

## 2026-07-24 — Distance sheet: exact-spec pass (fonts/colors/contrast)
**User feedback**: "not correct, need exact fonts styles & same design." Root cause: (1) sheet bg was white so white tiles didn't pop — design sheet is #F2F2F7 grey; (2) used SYSTEM colors instead of design hex.
**Fixes (all exact hex from distL HTML)**: sheet bg → systemGroupedBackground (#F2F2F7) so white tiles read as cards; title #1C1C1E, subtitle+labels #6D6D72, close X #8A8A8E on rgba(120,120,128,.14); tile numbers #0A84FF/#FF2D55/#E23B5A (19px w800 -0.3); card gradient #EAF2FF→#FDEEF3, border #5AB0FF@.25. Added design-palette constants in DistanceJourneyView.
**Verification**: BUILD SUCCEEDED, screenshot globe2.png — tiles now pop, colors exact, matches design.
**Files**: DistanceJourneyView.swift.

## 2026-07-24 — Distance km missing on real device: diagnosis + fallback UI
**Question**: km not showing on real-device Home. **Root cause**: distanceApartLabel is nil unless BOTH myLocation AND partnerLocation exist. Screenshot shows blue location arrow → this device IS capturing (myLocation present); partner (Nivedhithaa) hasn't shared location (her mood syncs, but Location is a separate opt-in permission she likely hasn't granted). NOT a sync bug — verified location fully wired: RType.location, saveLocation, syncNow→locationService.mergeRemote (mirrors moods).
**UX fix**: ClosenessStripView left half no longer vanishes when distance nil. New params hasMyLocation + onShareLocation. Shows: !hasMyLocation → "Share location ›" (tappable → location.requestAndCapture()); hasMyLocation but no partner → "Waiting for partner" (muted). Divider now always present.
**To get km**: partner opens app + allows Location (Settings→Tweli→Location→While Using); once her record syncs, km appears.
**Verification**: BUILD SUCCEEDED. Files: ClosenessStripView.swift, HomeMomentView.swift.

## 2026-07-24 — Realistic d3-orthographic Earth for "distance between us"
**What was done**: Replaced the fake blobby globe with a pixel-accurate Earth matching the design's Globe.jsx (d3.geoOrthographic, world-atlas 110m). Baked the projected coastlines + 15° graticule into `Tweli/Resources/GlobeGeometry.json` (offline via node d3-geo/topojson, scratchpad/bake.mjs) — a synchronized-group resource, auto-bundled (125 KB). New `GlobeGeometry.swift`: decodes the M/L/Z path strings once, plus a forward orthographic projector + geoInterpolate (verified to match d3 to 0.01px) for live pins/arc/plane. Rewrote `GlobeCanvas.swift` palette-driven (light/dark PALETTES: ocean gradient, glow halo, real land, graticule, edge shade, specular). `DistanceJourneyView.swift`: pins/great-circle gold arc (white underlay)/looping triangle plane now projected from real lon/lat (Puducherry↔Abu Dhabi); dark-mode card + title. Removed dead SVGMiniPath.swift.
**Decisions made**: Pre-bake land geometry (sidesteps re-implementing d3 spherical polygon clipping in Swift) but compute pins/arc/plane live so they stay correct. Globe orientation is fixed (matches design); city NAME labels stay dynamic.
**Verification hooks**: gated first-entry location AND notification prompts behind `TWELI_NO_LOCATION_ASK=1` (DEBUG only) so headless screenshots aren't covered. Verified light+dark on a fresh iPhone Air sim (SIMCTL_CHILD_TWELI_DEMO=1 TWELI_DISTANCE=1 TWELI_NO_LOCATION_ASK=1) — both match the reference frames 1:1.
**Gotcha**: sim permission dialogs raised on a pre-gate launch get STUCK (no touch injection to dismiss) and re-present every relaunch — must launch the gated build on a sim that never saw a pre-gate launch.
**Verification**: BUILD SUCCEEDED. Files: GlobeGeometry.swift (new), GlobeGeometry.json (new), GlobeCanvas.swift, DistanceJourneyView.swift, MainTabView.swift; SVGMiniPath.swift deleted.

## 2026-08-12 — Growth / Acquisition — invite funnel, rating prompt, landing page
**What was done**:
- Rewrote `public/join/index.html` (the invite landing page — the terminal node of the
  only viral loop). Removed the dead placeholder App Store link, fixed the 8-char code
  formatting to match `FirebaseService.formatPairCode`, added tap-to-copy, Open Graph /
  Twitter card meta so the link unfurls as a real invite in iMessage, a `tweli://` retry
  for people who already have the app, and a non-iOS branch.
- Rendered `public/og-invite.png` (1200x630 link-preview card) from a Chrome headless
  screenshot of a brand-matched HTML source.
- Added `Services/ReviewPromptService.swift` — arms an App Store rating ask at the two
  happy moments (partner joins, first letter opened), fires it in a LATER session at a
  calm point on Home. Wired into AppViewModel, TweliApp, MainTabView, OpenWhenLettersView.
- Rewrote `public/index.html` from a 26-line placeholder into a real landing page.

**Decisions made**:
- Install links use a single `INSTALL_URL = 'PENDING'` sentinel in both pages; while it is
  PENDING the CTA renders an honest "opening soon" state rather than a dead button.
- Rating prompt never fires in the session that armed it — first entry already spends two
  system modals (notification + location permission) and a third would stack.
- Left the landing page's screenshot slot EMPTY. The only captures in the repo show a
  five-tab bar and seeded letters; the app now ships four tabs and zero seeded content.

**Blockers**: Need the public TestFlight URL to replace the PENDING sentinel. Nothing is
deployed yet — the broken page is still live in production until `firebase deploy`.

**Next steps**: Paste TestFlight link -> swap sentinel in both pages -> deploy hosting.
Then analytics (no event instrumentation exists anywhere in the app).

**Files changed**: public/join/index.html, public/index.html, public/og-invite.png,
Tweli/Services/ReviewPromptService.swift, Tweli/App/AppViewModel.swift, Tweli/TweliApp.swift,
Tweli/Views/Main/MainTabView.swift, Tweli/Views/Letters/OpenWhenLettersView.swift

## 2026-08-12 — Smoke test of the acquisition changes
**What was done**: Verified the three shipped items end to end.
- Web: 15 scripted headless-Chrome assertions over both pages (code formatting incl.
  legacy 6-char, lowercase/dashed normalisation, missing-code state, PENDING never
  emitting an href, TestFlight/App Store CTA wiring, iOS vs non-iOS branch, tweli://
  retry target, OG tags absolute). 15/15 pass. Harness: scratchpad/smoke_web.py
- iOS: clean build; app installed and launched on iPhone 17 Pro on both the Letters and
  Home tabs. Renders correctly, no crash, no crash reports, no rating modal (correct —
  nothing armed). Console clean apart from a pre-existing UIBackgroundModes warning
  unrelated to these changes.
- Added TweliTests/ReviewPromptServiceTests.swift — 5 tests over the arm/fire state
  machine. Full suite: 22/22 pass (16 unit + 6 UI).

**Decisions made**: Gave ReviewPromptService two testability seams — an injectable
UserDefaults suite, and a closure-taking `fireIfArmed` overload (RequestReviewAction
cannot be constructed outside SwiftUI). The RequestReviewAction entry point forwards to it.

**Blockers**: Still need the public TestFlight URL; still not deployed, so the broken
invite page remains live in production.

**Known behaviour**: users already paired before this build get the prompt armed on their
first launch after updating (hasEverPartnered starts false), rather than at a real pairing
moment. Acceptable — they are the most engaged cohort — but gate it if undesired.

**Files changed**: Tweli/Services/ReviewPromptService.swift (testability seams),
TweliTests/ReviewPromptServiceTests.swift (new)

## 2026-08-12 — DEPLOYED to Firebase Hosting
**What was done**: `firebase deploy --only hosting` to tweli-9a99e (hosting ONLY —
functions and Firestore rules untouched). 3 new files uploaded.

**Verified in production**:
- Dead `apps.apple.com/app/tweli` link: GONE (0 occurrences on the served page).
- Rendered DOM: 8-char code shows `RKPM-7392`, legacy 6-char shows `ABC-123`.
- CTA carries NO href in the PENDING state (renders "Tweli is opening soon").
- `/.well-known/apple-app-site-association`: still 200 / application/json — universal
  links survived the deploy despite firebase.json's `**/.*` ignore rule.
- `/og-invite.png`: 200, image/png, 351KB.
- Landing page: 200, renders "Opening soon" / "Coming to iPhone".

**Still open**: INSTALL_URL is still the PENDING sentinel in both pages. The invite page
is now honest rather than broken, but there is still no install path for a partner who
does not have Tweli. Needs the public TestFlight URL, then a one-line change + redeploy.

## 2026-08-12 — Comp 0Z: entry tutorial (Z1/Z2/Z3)
**What was done**: Implemented the three-page animated entry tutorial from
"Twinderly A-Z.dc.html" (section 0Z).
- New `Tweli/Views/Onboarding/TutorialView.swift`: Z1 the thread drawing itself
  between the two dots, Z2 the floating partner-mood widget, Z3 the sealed letter
  with its "opens Dec 31" tag. Skip on every page; dots + CTA hoisted out of the
  pager so chrome stays put while pages swipe.
- Routed in `RootView` between the splash and SignInView, per the comp
  ("shown once after the splash, skippable any time").
- Persistence: `tweli.tutorialSeen`, with `AppViewModel.showTutorial` /
  `finishTutorial()`. Skip and "Start your thread" both finish.
- `AppEnvironment` TWELI_SKIP_ONBOARDING now also sets tutorialSeen.
- New DEBUG hook `TWELI_TUTORIAL_PAGE=<0-2>` (same shape as TWELI_TAB) so
  headless captures can reach pages 2 and 3 without touch injection.

**Decisions made**:
- Used `TweliGradient.sky(scheme)` rather than the comp's literal hexes. The
  comp's #16112A/#0D0A16/#160B12 IS sky(.dark), so this is faithful in dark and
  additionally coherent in light — matching SignInView, the screen it hands to.
- Art sits in a flexing region with the copy packed directly beneath it (comp's
  `flex:1`), NOT between two spacers — that split the slack and separated the
  picture from the sentence explaining it.
- Motion is suppressed under Reduce Motion; the thread renders fully drawn.
- `tweli.tutorialSeen` is deliberately NOT in resetLocalState()'s clear list: it
  teaches the app, not the account, so a user who deletes their account does not
  sit through it again.

**Verified**: build clean; all three pages captured on iPhone 17 Pro; thread
animation completes; TWELI_SKIP_ONBOARDING still lands on the tabs; full suite
22/22 pass.

**Files changed**: Tweli/Views/Onboarding/TutorialView.swift (new),
Tweli/App/RootView.swift, Tweli/App/AppViewModel.swift, Tweli/App/AppEnvironment.swift

## 2026-08-12 — Tutorial: proved "fresh install only"
**Why**: the 0Z tutorial had never been tested past a first launch. Every launch
during development was a fresh install (the container is wiped on reinstall), which
is precisely the case where the tutorial SHOULD appear — so the screenshots proved
nothing about the second launch.

**What was done**:
- Extracted the flag from an inline UserDefaults read in AppViewModel into
  `Services/TutorialGate.swift`, with an injectable defaults suite so the
  once-only contract is unit-testable.
- AppViewModel reads the gate exactly once, in init(). Re-reading it later would
  let the tutorial reappear mid-session if anything else touched UserDefaults.
- AppEnvironment now writes `TutorialGate.key` instead of a duplicated literal,
  so the debug launch hook and the gate cannot drift apart.
- New `TweliTests/TutorialGateTests.swift` — 5 tests: shown once then never again
  across 5 simulated relaunches; the flag is persisted rather than held in memory;
  marking twice is harmless; a genuine reinstall shows it again; the key matches
  the launch-override contract.

**Verified on device** (iPhone 17 Pro, one install, three launches):
- Launch 1, fresh install → tutorial page 1. Flag absent from the container.
- Flag set to 1 (exactly what markSeen() writes, asserted by the unit tests).
- Launch 2 → straight to SignInView, no tutorial.
- Launch 3 → straight to SignInView, no tutorial.

Full suite 27/27 pass.

**Files changed**: Tweli/Services/TutorialGate.swift (new),
TweliTests/TutorialGateTests.swift (new), Tweli/App/AppViewModel.swift,
Tweli/App/AppEnvironment.swift

## 2026-08-12 — Tutorial gate hardening + corrected verification method
**What was done**:
- `showTutorial` is now `@Published private(set)`, seeded by a property
  initializer (`= !TutorialGate().hasSeen`) rather than assigned in init().
  A property initializer runs once per AppViewModel construction — once per
  launch — and `private(set)` means `finishTutorial()` is the ONLY thing that
  can change it. "Shows once" is now compiler-enforced, not conventional.
- Removed the redundant read in init(), which was constructing a second
  TutorialGate purely because stored-property ordering forbade using the
  existing one.

**Correction to the previous entry's method**: the earlier "fresh install"
baseline was contaminated. `simctl uninstall` does NOT clear the app's
preference domain on the simulator, so a `defaults write` from an earlier run
survived the reinstall and the app skipped the tutorial on what looked like a
first launch. A true first-run test needs
`xcrun simctl spawn <dev> defaults delete <bundle-id>` first.
Re-verified cleanly, both directions, same binary:
- domain cleared -> launch -> tutorial page 1
- flag written  -> launch -> straight to SignInView

Build clean, suite 27/27.

**Files changed**: Tweli/App/AppViewModel.swift

## 2026-08-16 — Reinstall recovery / Implementation
**What was done**: Built comps K1–K5 and KL1–KL5 (light). Added ReinstallGate (Keychain marker surviving app deletion), a typed FirebaseService.RestoreOutcome, the K2 restore state machine in AppViewModel, and four new screens under Views/Restore. Fixed the E6 partner-name regression caused by the leaveSpace function clearing memberNames.
**Decisions made**: Keychain stores a flag only (no partner name) — user's call; K1 says "them" until sign-in. K4 shows only what iOS actually wiped, checked via WidgetCenter + UNUserNotificationCenter. K5 is only reachable from a SUCCESSFUL query — `.unreachable` never claims a partner left.
**Blockers**: None.
**Next steps**: Verify K1→K2→K3 end-to-end on a real device with a real uninstall (the simulator can stage the state but not the actual delete).
**Files changed**: Tweli/Services/{ReinstallGate,ReinstallRestore,FirebaseService,WidgetDataService,ReminderNotificationService}.swift, Tweli/App/{AppViewModel,RootView,AppEnvironment}.swift, Tweli/Views/Restore/*, Tweli/Views/Auth/SignInView.swift, TweliTests/{ReinstallGateTests,RestoreCountingTests}.swift
